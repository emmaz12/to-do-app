import React, {useState} from 'react';
import styles from '../styles/input.module.css';

const Input = () => {

    return (
        <>
            <input className={styles.input} type="text" placeholder="I need to..."/>
            
        </>
    );
};

export default Input;
